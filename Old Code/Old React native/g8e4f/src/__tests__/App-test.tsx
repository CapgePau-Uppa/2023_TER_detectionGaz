/**
 * @format
 */

import 'react-native';
// import React from 'react';
// import App from '../App';

// Note: test renderer must be required after react-native.
// import renderer from 'react-test-renderer';

it('passes dummy test', () => {
  // TODO: Add tests are adding some for other components
  // renderer.create(<App />);
  expect(1 + 2).toBe(3);
});
